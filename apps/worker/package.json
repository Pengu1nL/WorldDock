{
  "name": "@worlddock/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/src/main.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@worlddock/config": "workspace:*",
    "@worlddock/db": "workspace:*",
    "@worlddock/domain": "workspace:*",
    "bullmq": "^5.77.3"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
